
import { StyleSheet, View } from "react-native"

import { BtDef } from "../BtDef"

export const Produto = () => {

    return(
        <View style={styles.container}>
        </View>
    )
}

const styles = StyleSheet.create({
    container: {
        flex:1,
        flexDirection: row,
    }
})